link(rel="import" href="#{ELEMENT_PATH}/ls-lovepage.html")

ls-lovepage(
  url="#{love_page_url(@love_page)}"
  love_page="#{@love_page.to_json}"
)

scss:
  html {
    background: rgba(238, 238, 238, 0.4);
  }

  a {
    text-decoration: none;
  }

  a:visited, a:target {
    text-decoration: none;
  }
